# Exclude VCS metadata
.gcloudignore
.git
.gitignore

# Reuse project ignore rules, then explicitly include generated router tree needed for production builds.
#!include:.gitignore
!src/routeTree.gen.ts
